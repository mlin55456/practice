# 倉儲管理系統 - 部署指南

## 🎯 部署方案概覽

### 📊 方案比較表

| 部署方式 | 適用場景 | 技術難度 | 成本 | 使用者數量 | 推薦度 |
|---------|---------|---------|------|-----------|--------|
| 本地網路共享 | 辦公室內部使用 | ⭐ | 免費 | 5-20人 | ⭐⭐⭐⭐ |
| 可執行檔分發 | 各自獨立使用 | ⭐⭐ | 免費 | 不限 | ⭐⭐⭐⭐⭐ |
| Docker 部署 | 伺服器環境 | ⭐⭐⭐ | 低 | 50+人 | ⭐⭐⭐⭐ |
| 雲端部署 | 遠端多人協作 | ⭐⭐⭐⭐ | 付費 | 不限 | ⭐⭐⭐ |

---

## 🏠 方案一：本地網路共享 (推薦給辦公室)

### 🎯 適用場景
- 同一辦公室或家庭網路
- 5-20 人同時使用
- 需要資料集中管理
- 不需要外網存取

### 🚀 快速部署

#### 1. 啟動伺服器模式
```bash
# 雙擊執行
start-server.bat

# 或手動執行
mvn spring-boot:run -Dspring-boot.run.profiles=production
```

#### 2. 系統會自動顯示存取資訊
```
========================================
系統啟動成功！
========================================

本機存取: http://localhost:8080
區域網路存取: http://192.168.1.100:8080
資料庫控制台: http://192.168.1.100:8080/h2-console

其他人可以透過以下網址存取系統：
http://192.168.1.100:8080
```

#### 3. 防火牆設定 (Windows)
```bash
# 開啟 Windows 防火牆例外
netsh advfirewall firewall add rule name="倉儲管理系統" dir=in action=allow protocol=TCP localport=8080
```

### ✅ 優點
- 設定超簡單，一鍵啟動
- 資料集中管理，即時同步
- 所有人看到相同資料
- 支援多人同時操作

### ❌ 缺點
- 只能內網存取
- 主機必須持續開機
- 網路依賴性高

---

## 📦 方案二：可執行檔分發 (最推薦)

### 🎯 適用場景
- 各部門獨立使用
- 離線環境工作
- 簡單分發給多人
- 不需要共享資料

### 🚀 建立分發包

#### 1. 執行打包腳本
```bash
# 雙擊執行
build-distribution.bat
```

#### 2. 分發包內容
```
distribution/
├── inventory-management.jar    # 主程式
├── start.bat                  # Windows 啟動腳本
├── start.sh                   # Linux/Mac 啟動腳本
├── README.txt                 # 使用說明
└── data/                      # 資料目錄
    └── backup/               # 備份目錄
```

#### 3. 分發給其他人
- 將整個 `distribution` 目錄壓縮
- 傳送給需要的人
- 解壓後雙擊 `start.bat` 即可使用

### 📋 使用者操作步驟
1. 解壓分發包到任意目錄
2. 雙擊 `start.bat` (Windows) 或執行 `./start.sh` (Linux/Mac)
3. 瀏覽器開啟 http://localhost:8080
4. 開始使用系統

### ✅ 優點
- 完全獨立運行
- 離線可用
- 資料隔離安全
- 分發超簡單
- 不需要網路設定

### ❌ 缺點
- 資料不共享
- 需要 Java 環境

---

## 🐳 方案三：Docker 部署 (適合 IT 人員)

### 🎯 適用場景
- 有 Docker 環境
- 需要容器化部署
- 伺服器環境運行

### 🚀 Docker 部署步驟

#### 1. 建立 Docker 映像
```bash
# 編譯專案
mvn clean package -DskipTests

# 建立 Docker 映像
docker build -t inventory-management .
```

#### 2. 使用 Docker Compose 啟動
```bash
# 啟動服務
docker-compose up -d

# 查看狀態
docker-compose ps

# 查看日誌
docker-compose logs -f
```

#### 3. 存取系統
- 網址：http://localhost:8080
- 資料會持久化在 `./data` 目錄

### ✅ 優點
- 環境隔離
- 易於部署和管理
- 支援自動重啟
- 資源控制

---

## ☁️ 方案四：雲端部署

### 🎯 Heroku 部署 (免費方案)

#### 1. 準備 Heroku 配置
```bash
# 安裝 Heroku CLI
# 登入 Heroku
heroku login

# 建立應用程式
heroku create your-inventory-app

# 部署
git add .
git commit -m "Deploy to Heroku"
git push heroku main
```

#### 2. 設定環境變數
```bash
heroku config:set SPRING_PROFILES_ACTIVE=production
```

### 🎯 AWS EC2 部署

#### 1. 啟動 EC2 實例
- 選擇 Amazon Linux 2
- 安裝 Java 8
- 開放 8080 埠口

#### 2. 部署應用程式
```bash
# 上傳 JAR 檔案
scp inventory-management.jar ec2-user@your-server:/home/ec2-user/

# SSH 連線到伺服器
ssh ec2-user@your-server

# 啟動應用程式
nohup java -jar inventory-management.jar &
```

---

## 🔧 進階設定

### 🔒 安全性設定

#### 1. 修改預設埠口
```properties
# application-production.properties
server.port=9090
```

#### 2. 限制存取 IP
```properties
# 只允許特定 IP 存取
server.address=192.168.1.100
```

#### 3. 關閉 H2 控制台 (生產環境)
```properties
spring.h2.console.enabled=false
```

### ⚡ 效能優化

#### 1. JVM 參數調整
```bash
java -Xmx1g -Xms512m -jar inventory-management.jar
```

#### 2. 資料庫連線池
```properties
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.minimum-idle=10
```

### 📊 監控設定

#### 1. 啟用 Actuator
```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-actuator</artifactId>
</dependency>
```

#### 2. 健康檢查端點
- http://localhost:8080/actuator/health
- http://localhost:8080/actuator/metrics

---

## 🆘 故障排除

### 常見問題解決

#### 1. 埠口被占用
```bash
# Windows 查看埠口使用
netstat -ano | findstr :8080

# 終止占用程序
taskkill /PID <PID> /F
```

#### 2. Java 版本問題
```bash
# 檢查 Java 版本
java -version

# 需要 Java 8 或更高版本
```

#### 3. 記憶體不足
```bash
# 增加 JVM 記憶體
java -Xmx2g -jar inventory-management.jar
```

#### 4. 資料庫檔案損壞
```bash
# 從備份恢復
# 檢查 data/backup/ 目錄中的 CSV 檔案
```

---

## 📞 技術支援清單

### 部署前檢查
- [ ] Java 8+ 已安裝
- [ ] 埠口 8080 可用
- [ ] 防火牆設定正確
- [ ] 磁碟空間充足 (>100MB)

### 網路共享檢查
- [ ] 所有電腦在同一網段
- [ ] 防火牆允許 8080 埠口
- [ ] IP 位址正確

### 分發包檢查
- [ ] JAR 檔案完整
- [ ] 啟動腳本可執行
- [ ] 資料目錄存在

---

## 🎉 推薦部署方案

### 🏢 辦公室團隊 (5-20人)
**推薦：方案一 - 本地網路共享**
- 執行 `start-server.bat`
- 所有人存取同一個網址
- 資料即時同步

### 👥 多部門使用
**推薦：方案二 - 可執行檔分發**
- 執行 `build-distribution.bat`
- 分發給各部門
- 各自獨立使用

### 🌐 遠端團隊
**推薦：方案四 - 雲端部署**
- 部署到 Heroku 或 AWS
- 提供公網存取
- 專業級服務

選擇最適合你的方案，開始享受高效的倉儲管理！